const pool = require('./config/db'); pool.query("INSERT INTO messages (chat_id, sender_uid, text, image_url, video_url) VALUES ('test_chat', 'test_uid', '', 'test.jpg', null) RETURNING *").then(res => {console.log('Inserted:', res.rows); return pool.query("DELETE FROM messages WHERE chat_id='test_chat'");}).then(() => {console.log('Deleted'); process.exit();}).catch(console.error);
